# Local development / testing only — Vercel installs requirements.txt,
# which is kept lean to stay well under the serverless bundle size limit.
-r requirements.txt

uvicorn[standard]==0.29.0
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0
